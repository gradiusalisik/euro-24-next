import styled from "styled-components";
import { colors } from "../../variables";

export const TitleStyled = styled.h2`
  margin-bottom: 40px;
  font-size: 40px;
  line-height: 1.4;
  text-align: center;
  color: ${colors.main};
`;
